diff --git a/CMakeLists.txt b/CMakeLists.txt
index 33f7c90a..4aa885de 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -193,6 +193,18 @@ endif (OM_COPY_LIBS)
 # add_executable (pVivax model/Pv_mod/Source.cpp)
 # target_link_libraries (pVivax libpvivax)
 
+# -----  Python bindings (nanobind)  -----
+#
+# Not part of upstream/this fork: this hook is applied locally (see
+# ../patches/0001-add-python-bindings-hook.patch) by the openmalaria-python
+# repo's own build step, so this submodule's checked-in CMakeLists.txt stays
+# byte-for-byte identical to the openmalaria-fork core-only branch.
+
+option (OM_BUILD_PYTHON "Build nanobind-based Python bindings (openmalaria Python package)" OFF)
+if (OM_BUILD_PYTHON)
+  add_subdirectory (${CMAKE_CURRENT_LIST_DIR}/../bindings ${CMAKE_BINARY_DIR}/bindings)
+endif (OM_BUILD_PYTHON)
+
 
 # -----  OM_BOXTEST - black-box & unit testing  -----
 
